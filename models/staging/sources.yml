version: 2

# Bronze layer: source-aligned data lives in raw (ingestion / dbt seed).
sources:
  - name: finance_source
    schema: raw
    tables:
      - name: raw_customers
      - name: raw_accounts
      - name: raw_transactions
      - name: raw_organizations
